import { css } from 'styled-components';

export const FontFamily = css`
  font-family: Roboto;
`;

export const FontWeight = css`
  --light: 300;
  --normal: normal;
  --medium: 500;
  --semi-bold: 600;
  --bold: bold;
`;
